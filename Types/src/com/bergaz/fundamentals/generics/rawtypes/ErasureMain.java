package com.bergaz.fundamentals.generics.rawtypes;

public class ErasureMain {
    public static void main(String[] args) {
        /**
         * Translation at compile time, not runtime
         * Generic representation is a bit of a lie it only exists in source code
         * After compilation we can see raw types are in use
         */
    }
}
